---
layout: default
---

## 作品集

**キーワード：** <%= @keywords.join("、")%>

<% @content_data.each do |theme, projects| %>

<% projects.each do |project| %>
  <% name = project.first %>
  <% info = project.last %>

# <%= name %>

<%= info["description"] %>

<% unless info["items"].nil? %>
<%= info["items"].map { |item| "- #{item}" }.join("\n") %>
<% end %>
    
<% unless info["image"].nil? %>
!["<%= name %>"](<%= info["image"] %>)
<% end %>

| --- | --- |
| 成果物 | <%= info["output"] %> |
| 言語 | <%= info["lang"] %> |
| カテゴリ | <%= theme %>・<%= info["category"] %> |
| リンク | <<%= info["url"] %>> |

<% end %>
<% end %>
